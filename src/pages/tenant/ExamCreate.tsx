import React from 'react';
import { ExamSchedulingWizard } from '@/components/tenant/exam-wizard';

const ExamCreate: React.FC = () => {
  return <ExamSchedulingWizard />;
};

export default ExamCreate;
